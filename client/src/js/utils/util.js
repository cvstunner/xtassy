// // Get all elements with the class 'scale-hover'
// const scaleHoverElements = document.querySelectorAll('.scale-hover');
//
// // Add event listeners to each element
// scaleHoverElements.forEach(element => {
//   // On mouseover, add the hover-active class to start the animation
//   element.addEventListener('mouseover', () => {
//     if (!element.classList.contains('hover-active')) {
//       element.classList.add('hover-active');
//     }
//   });
//
//   // On mouseout, you can remove the class if you want to reset the animation
//   element.addEventListener('mouseout', () => {
//     // Optionally remove the class after some delay if needed
//     setTimeout(() => {
//       element.classList.remove('hover-active');
//     }, 1000); // Adjust delay to match animation duration
//   });
// });
